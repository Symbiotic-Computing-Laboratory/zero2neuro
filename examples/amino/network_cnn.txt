# Convolutional Neural Network

--network_type=cnn
# String input: accept only a single string for an example
--input_shape
1

# 4 convolutional modules: each is defined by a kernel of some size,
#  number of filters, and a possible max pooling step
--conv_kernel_size
5
5
5
5
--conv_padding=valid
--conv_number_filters
64
128
128
256
--conv_activation=elu

# The last convolutional module does not perform pooling because
#  it is immediately followed by a GlobalMaxPooling step
--conv_pool_size
0
2
0
0

# After GlobalMaxPool: no more spatial information
--number_hidden_units
50
25
10
--hidden_activation=elu
--output_shape
1

# elp1 = elu+1 (output is non-negative)
--output_activation=elup1

--dropout=0.5
--spatial_dropout=0.1
--l2=0.0002
