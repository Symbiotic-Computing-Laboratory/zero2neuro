# Mosquito dataset — Scenario A (with prior)
# Data format: per-location (year × epiweek × features) grid, assembled into folds.
#
# Each training example is one (location, year) pair:
#   sequence length = 30 epiweeks
#   features        = 47 bio/weather and lands features
#   (year and epiweek are encoded as array axes, not feature values)
#
# Run prepare_data_prior.py first to generate the fold pkl files in ./dataset/

--data_format=pickle
--data_representation=numpy
--data_fold_split=identity
--data_set_type=holistic-cross-validation

--dataset_directory=dataset

--data_n_folds=5

--data_files
fold_0.pkl
fold_1.pkl
fold_2.pkl
fold_3.pkl
fold_4.pkl

# ---- input features (47 biological/environmental) ----
--data_inputs
X.Green.RS.
X.Water.RS.
Daily.Mean..C..RS.
Daily.Max..C..RS.
Daily.Min..C..RS.
Day.Min..C..RS.
Night.Min..C..RS.
Max.Wind.Speed.M.
Solar.Radiation.MJ.m2..M.
Mean.Humidity.M.
Precip.RS.
Mean.Soil.Moisture.M.
Culex.pipiens.prior
data_mask

# ---- prediction target ----
--data_outputs
Culex.pipiens

#--data_weights
#data_mask
