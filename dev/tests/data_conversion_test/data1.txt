--data_format=tabular

# Each data table is converted from a numpy array to a tf-dataset
--data_representation=tf-dataset

# Format: data file, fold # (-1 = none specified), sheet name
--data_file=xor_data.xlsx,-1,xor_data_2

# Header is on row 2
--tabular_header_row=2

# Use columns 3..5
--tabular_column_range
3
5
--data_set_type=fixed
--data_inputs
In 0
In 1
--data_outputs
Out 0
